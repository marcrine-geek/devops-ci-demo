# PDS DevOps CI Demo (Node)

Tiny Express service used for a CI/CD assessment. No deployments required.

## Local run
```bash
npm install
export DEMO_API_KEY="not-a-real-secret"
npm test
npm run lint
npm start
